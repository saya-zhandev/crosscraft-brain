# Keep the build context small and never leak local secrets/artifacts into the image.
**/node_modules
**/.next
**/dist
**/.turbo
.git
.env
*.log
.DS_Store
**/*.tsbuildinfo
# local Go dev binary (the image builds its own)
server/crosscraft
server/crosscraft.exe
Dockerfile
.dockerignore
